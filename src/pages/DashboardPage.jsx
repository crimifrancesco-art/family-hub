import React from 'react'
import { useApp } from '../context/AppContext'
export default function DashboardPage() { const { docs, meds, visits, trips } = useApp(); const expiringDocs = docs.filter((d) => d.expiry); const activeTrip = trips[0]; return <section><div className="g4"><div className="stat accent"><div className="stat-n">{docs.length}</div><div className="stat-l"><i className="ti ti-folder" /> Documenti</div></div><div className="stat"><div className="stat-n">{meds.length}</div><div className="stat-l"><i className="ti ti-pill" /> Farmaci</div></div><div className="stat"><div className="stat-n">{visits.length}</div><div className="stat-l"><i className="ti ti-stethoscope" /> Visite</div></div><div className="stat"><div className="stat-n">{trips.length}</div><div className="stat-l"><i className="ti ti-plane" /> Viaggi</div></div></div><div className="g2"><div className="card"><div className="card-title"><i className="ti ti-alert-circle" /> Scadenze</div>{expiringDocs.length ? expiringDocs.map((d) => <div key={d.id} className="ri"><div className="rw">{d.expiry}</div><div>{d.name}</div></div>) : <div className="empty"><i className="ti ti-calendar-off" /> Nessuna scadenza nei prossimi 90 giorni</div>}</div><div className="card"><div className="card-title"><i className="ti ti-plane" /> Prossimo viaggio</div>{activeTrip ? <div><div className="trip-nm">{activeTrip.emoji} {activeTrip.name}</div><div className="mem-row" style={{ marginTop: 8 }}><i className="ti ti-calendar-event" /> {activeTrip.dateFrom} → {activeTrip.dateTo}</div><div className="mem-row"><i className="ti ti-wallet" /> Budget € {activeTrip.budget}</div></div> : <div className="empty"><i className="ti ti-plane-off" /> Nessun viaggio pianificato</div>}</div></div></section> }
