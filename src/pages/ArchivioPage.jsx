import React, { useState } from 'react'
import { useApp } from '../context/AppContext'
export default function ArchivioPage() { const { docs, family, addDoc, removeDoc } = useApp(); const [open, setOpen] = useState(false); const [form, setForm] = useState({ name: '', type: 'pdf', category: 'Identità', person: family[0]?.id || '', date: '', expiry: '', size: '' }); const submit = () => { if (!form.name.trim()) return; addDoc(form); setForm({ name: '', type: 'pdf', category: 'Identità', person: family[0]?.id || '', date: '', expiry: '', size: '' }); setOpen(false) }; return <section><div className="sh"><div className="st"><i className="ti ti-folder" /> Archivio</div><button className="btn btn-p" onClick={() => setOpen(true)}><i className="ti ti-plus" /> Aggiungi documento</button></div><div className="card">{docs.length ? docs.map((d) => <div key={d.id} className="doc-item"><div className={`doc-ic ${d.type}`}><i className={d.type === 'pdf' ? 'ti ti-file-type-pdf' : d.type === 'img' ? 'ti ti-photo' : 'ti ti-notes'} /></div><div className="doc-nm">{d.name}</div><div className="rw">{d.date}</div><button className="btn btn-s btn-d" onClick={() => removeDoc(d.id)}>Elimina</button></div>) : <div className="empty"><i className="ti ti-folder-off" /> Nessun documento trovato</div>}</div>{open && <Modal title="Nuovo documento" onClose={() => setOpen(false)}><Field label="Nome documento"><input className="fi" value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} /></Field><div className="fr"><Field label="Tipo"><select className="fi" value={form.type} onChange={(e) => setForm({ ...form, type: e.target.value })}><option value="pdf">PDF</option><option value="img">Immagine</option><option value="note">Nota</option></select></Field><Field label="Categoria"><input className="fi" value={form.category} onChange={(e) => setForm({ ...form, category: e.target.value })} /></Field></div><div className="fr"><Field label="Data"><input type="date" className="fi" value={form.date} onChange={(e) => setForm({ ...form, date: e.target.value })} /></Field><Field label="Scadenza"><input type="date" className="fi" value={form.expiry} onChange={(e) => setForm({ ...form, expiry: e.target.value })} /></Field></div><div className="fa"><button className="btn" onClick={() => setOpen(false)}>Annulla</button><button className="btn btn-p" onClick={submit}>Salva</button></div></Modal>}</section> }
function Modal({ title, onClose, children }) { return <div className="overlay" onClick={onClose}><div className="modal" onClick={(e) => e.stopPropagation()}><div className="modal-t">{title}</div>{children}</div></div> }
function Field({ label, children }) { return <div className="fg"><label className="fl">{label}</label>{children}</div> }
