import { useMemo, useState } from 'react'
import { AIRLINE_DIRECTORY, TRIP_STATUS_OPTIONS, useAppContext } from '../context/AppContext'

function labelStatus(value) { return TRIP_STATUS_OPTIONS.find((item) => item.value === value)?.label || value }
function mapsLink(address, lat, lng) { if (lat && lng) return `https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(`${lat},${lng}`)}`; return address ? `https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(address)}` : '' }
function directionsLink(address, lat, lng) { const destination = lat && lng ? `${lat},${lng}` : address; if (!destination) return ''; return `https://www.google.com/maps/dir/?api=1&destination=${encodeURIComponent(destination)}` }
function googleCalendarLink({ title, startDate, endDate, details = '', location = '' }) { if (!startDate) return ''; const start = `${startDate.replaceAll('-', '')}T090000`; const end = `${(endDate || startDate).replaceAll('-', '')}T100000`; return `https://calendar.google.com/calendar/render?action=TEMPLATE&text=${encodeURIComponent(title)}&dates=${start}/${end}&details=${encodeURIComponent(details)}&location=${encodeURIComponent(location)}` }
function youtubeThumb(url) { const m = url?.match(/(?:v=|youtu\.be\/)([A-Za-z0-9_-]{11})/); return m ? `https://img.youtube.com/vi/${m[1]}/hqdefault.jpg` : '' }
function guessMediaThumb(type, url, thumb) { if (thumb) return thumb; if (type === 'youtube') return youtubeThumb(url); return '' }
function EmptyState({ icon, text }) { return <div className="empty"><i className={icon} /> <p>{text}</p></div> }
function Modal({ title, onClose, children }) { return <div className="overlay" onClick={onClose}><div className="modal" onClick={(e) => e.stopPropagation()}><div className="modal-t">{title}</div>{children}</div></div> }
function CardActions({ children }) { return <div className="actions-row">{children}</div> }

export default function ViaggiPage() {
  const { trips, familyMembers, addTrip, updateTrip, deleteTrip, toggleTripMember, addFlight, updateFlight, deleteFlight, invertFlightRoute, addFlightBaggage, deleteFlightBaggage, addFlightDeadline, deleteFlightDeadline, addHotel, updateHotel, deleteHotel, addHotelDeadline, deleteHotelDeadline, addParkingReservation, deleteParkingReservation, addCarRental, deleteCarRental, addDiaryDay, deleteDiaryDay, addDiaryPlace, deleteDiaryPlace, addDiaryMedia, deleteDiaryMedia, toggleChecklistItem, addChecklistItem, removeChecklistItem } = useAppContext()
  const [selectedTripId, setSelectedTripId] = useState(trips[0]?.id || null)
  const [activeTab, setActiveTab] = useState('flights')
  const [modal, setModal] = useState(null)
  const selectedTrip = useMemo(() => trips.find((trip) => trip.id === selectedTripId) || trips[0] || null, [trips, selectedTripId])
  const openModal = (type, item = null, parentId = null) => setModal({ type, item, parentId })
  const closeModal = () => setModal(null)

  const fillCurrentPosition = () => {
    if (!navigator.geolocation) return alert('Geolocalizzazione non supportata')
    navigator.geolocation.getCurrentPosition((pos) => {
      const lat = String(pos.coords.latitude.toFixed(6))
      const lng = String(pos.coords.longitude.toFixed(6))
      const latInput = document.querySelector('input[name="lat"]')
      const lngInput = document.querySelector('input[name="lng"]')
      if (latInput) latInput.value = lat
      if (lngInput) lngInput.value = lng
    }, () => alert('Impossibile ottenere la posizione attuale'))
  }

  const submitTrip = (e) => { e.preventDefault(); const fd = new FormData(e.currentTarget); const payload = { name: fd.get('name'), status: fd.get('status'), dateFrom: fd.get('dateFrom'), dateTo: fd.get('dateTo') }; if (modal?.item) updateTrip(modal.item.id, payload); else addTrip(payload); closeModal() }
  const submitFlight = (e) => { e.preventDefault(); const fd = new FormData(e.currentTarget); const payload = { company: fd.get('company'), from: fd.get('from'), to: fd.get('to'), date: fd.get('date'), departureTime: fd.get('departureTime'), arrivalTime: fd.get('arrivalTime'), flightNumber: fd.get('flightNumber'), bookingRef: fd.get('bookingRef'), purchaseCost: fd.get('purchaseCost'), companyUrl: AIRLINE_DIRECTORY[fd.get('company')] || '' }; if (modal?.item) updateFlight(selectedTrip.id, modal.item.id, payload); else addFlight(selectedTrip.id, payload); closeModal() }
  const submitFlightBag = (e) => { e.preventDefault(); const fd = new FormData(e.currentTarget); addFlightBaggage(selectedTrip.id, modal.parentId, { label: fd.get('label'), qty: fd.get('qty'), cost: fd.get('cost') }); closeModal() }
  const submitFlightDeadline = (e) => { e.preventDefault(); const fd = new FormData(e.currentTarget); addFlightDeadline(selectedTrip.id, modal.parentId, { title: fd.get('title'), date: fd.get('date'), notes: fd.get('notes') }); closeModal() }
  const submitHotel = (e) => { e.preventDefault(); const fd = new FormData(e.currentTarget); const payload = { name: fd.get('name'), phone: fd.get('phone'), address: fd.get('address'), lat: fd.get('lat'), lng: fd.get('lng'), bookingUrl: fd.get('bookingUrl'), alternateUrl: fd.get('alternateUrl'), checkIn: fd.get('checkIn'), checkOut: fd.get('checkOut'), paidAmount: fd.get('paidAmount'), dueAmount: fd.get('dueAmount'), paymentMethod: fd.get('paymentMethod'), cancellationDate: fd.get('cancellationDate') }; if (modal?.item) updateHotel(selectedTrip.id, modal.item.id, payload); else addHotel(selectedTrip.id, payload); closeModal() }
  const submitHotelDeadline = (e) => { e.preventDefault(); const fd = new FormData(e.currentTarget); addHotelDeadline(selectedTrip.id, modal.parentId, { title: fd.get('title'), date: fd.get('date'), notes: fd.get('notes') }); closeModal() }
  const submitParking = (e) => { e.preventDefault(); const fd = new FormData(e.currentTarget); addParkingReservation(selectedTrip.id, { name: fd.get('name'), address: fd.get('address'), dateFrom: fd.get('dateFrom'), dateTo: fd.get('dateTo'), cost: fd.get('cost'), bookingUrl: fd.get('bookingUrl'), notes: fd.get('notes') }); closeModal() }
  const submitCarRental = (e) => { e.preventDefault(); const fd = new FormData(e.currentTarget); addCarRental(selectedTrip.id, { company: fd.get('company'), pickupPlace: fd.get('pickupPlace'), dropoffPlace: fd.get('dropoffPlace'), pickupDate: fd.get('pickupDate'), dropoffDate: fd.get('dropoffDate'), cost: fd.get('cost'), deposit: fd.get('deposit'), bookingUrl: fd.get('bookingUrl'), notes: fd.get('notes') }); closeModal() }
  const submitDiaryDay = (e) => { e.preventDefault(); const fd = new FormData(e.currentTarget); addDiaryDay(selectedTrip.id, { date: fd.get('date'), title: fd.get('title'), notes: fd.get('notes') }); closeModal() }
  const submitPlace = (e) => { e.preventDefault(); const fd = new FormData(e.currentTarget); addDiaryPlace(selectedTrip.id, { type: fd.get('type'), name: fd.get('name'), address: fd.get('address'), lat: fd.get('lat'), lng: fd.get('lng'), url: fd.get('url'), rating: fd.get('rating'), reviewNote: fd.get('reviewNote'), imageUrl: fd.get('imageUrl'), notes: fd.get('notes') }); closeModal() }
  const submitMedia = (e) => { e.preventDefault(); const fd = new FormData(e.currentTarget); const type = fd.get('type'); const url = fd.get('url'); addDiaryMedia(selectedTrip.id, { type, title: fd.get('title'), url, source: fd.get('source'), thumbnail: guessMediaThumb(type, url, fd.get('thumbnail')), notes: fd.get('notes') }); closeModal() }
  const submitChecklistItem = (e) => { e.preventDefault(); const fd = new FormData(e.currentTarget); addChecklistItem(selectedTrip.id, modal.parentId, fd.get('label')); closeModal() }

  return <div className="page-stack">
    <section className="hero-card travel-hero"><div><div className="eyebrow">Viaggi</div><h1>Planner di viaggio smart per tutta la famiglia</h1><p>Più rapido su cellulare, con geolocalizzazione, diario ricco, reminder valigie, logistica e membri partecipanti selezionabili con un tap.</p></div><button className="btn btn-p" onClick={() => openModal('trip')}>+ Aggiungi viaggio</button></section>

    <div className="trip-grid">{trips.map((trip) => <article key={trip.id} className={`trip ${selectedTrip?.id === trip.id ? 'trip-selected' : ''}`}><div className="trip-h clickable" onClick={() => setSelectedTripId(trip.id)}><div className="trip-nm">{trip.name}</div><div className="meta-line">{trip.dateFrom} → {trip.dateTo}</div><div className="pill-row"><span className="pill pill-a">{labelStatus(trip.status)}</span></div></div><div className="trip-ft"><button className="btn btn-s" onClick={() => openModal('trip', trip)}>Modifica</button><button className="btn btn-s btn-d" onClick={() => deleteTrip(trip.id)}>Elimina</button></div></article>)}</div>

    {selectedTrip && <>
      <section className="card trip-summary-card glass-card">
        <div className="summary-top"><div><div className="trip-nm">{selectedTrip.name}</div><div className="meta-line">{selectedTrip.dateFrom} → {selectedTrip.dateTo}</div></div><span className="pill pill-g">{labelStatus(selectedTrip.status)}</span></div>
        <div className="member-selector">{familyMembers.map((member) => <button key={member.id} className={`member-avatar ${selectedTrip.persons.includes(member.id) ? 'active' : ''}`} onClick={() => toggleTripMember(selectedTrip.id, member.id)} title={member.name}><span>{member.initials}</span><small>{member.name}</small></button>)}</div>
        <div className="itabs scroll-tabs"><button className={`itab ${activeTab === 'flights' ? 'active' : ''}`} onClick={() => setActiveTab('flights')}>Voli</button><button className={`itab ${activeTab === 'hotels' ? 'active' : ''}`} onClick={() => setActiveTab('hotels')}>Hotel</button><button className={`itab ${activeTab === 'logistics' ? 'active' : ''}`} onClick={() => setActiveTab('logistics')}>Parcheggio & Auto</button><button className={`itab ${activeTab === 'diary' ? 'active' : ''}`} onClick={() => setActiveTab('diary')}>Diario smart</button><button className={`itab ${activeTab === 'checklist' ? 'active' : ''}`} onClick={() => setActiveTab('checklist')}>Cose da portare</button></div>
      </section>

      {activeTab === 'flights' && <section><div className="sh wrap"><div className="st">Voli</div><button className="btn btn-p" onClick={() => openModal('flight')}>+ Aggiungi volo</button></div>{!selectedTrip.flights.length && <EmptyState icon="ti ti-plane-off" text="Nessun volo — clicca Aggiungi" />}<div className="grid-cards cols-1">{selectedTrip.flights.map((flight) => <div key={flight.id} className="card stack-card"><div className="card-head between wrap"><div><div className="trip-nm small">{flight.company || 'Volo'}</div><div className="meta-line">{flight.from} → {flight.to}</div></div><div className="meta-group"><span>{flight.date}</span><span>{flight.departureTime} - {flight.arrivalTime}</span></div></div><div className="detail-grid cols-3"><div><strong>Numero</strong><span>{flight.flightNumber || '—'}</span></div><div><strong>Prenotazione</strong><span>{flight.bookingRef || '—'}</span></div><div><strong>Costo</strong><span>{flight.purchaseCost ? `€ ${flight.purchaseCost}` : '—'}</span></div></div><CardActions><button className="btn btn-s" onClick={() => openModal('flight', flight)}>Modifica</button><button className="btn btn-s" onClick={() => invertFlightRoute(selectedTrip.id, flight.id)}>Inverti tratta</button>{flight.companyUrl && <a className="btn btn-s" href={flight.companyUrl} target="_blank" rel="noreferrer">Compagnia</a>}<button className="btn btn-s btn-d" onClick={() => deleteFlight(selectedTrip.id, flight.id)}>Elimina</button></CardActions><div className="subsection"><div className="subsection-head between wrap"><h4>Bagagli</h4><button className="btn btn-s" onClick={() => openModal('flight-bag', null, flight.id)}>+ Aggiungi bagaglio</button></div>{!flight.baggage?.length && <p className="muted">Nessun bagaglio registrato.</p>}<div className="chips-list">{(flight.baggage || []).map((bag) => <div key={bag.id} className="chip-card"><span>{bag.label} · Qty {bag.qty} · {bag.cost ? `€ ${bag.cost}` : 'senza costo'}</span><button className="btn-icon danger" onClick={() => deleteFlightBaggage(selectedTrip.id, flight.id, bag.id)}><i className="ti ti-trash" /></button></div>)}</div></div><div className="subsection"><div className="subsection-head between wrap"><h4>Scadenze volo</h4><button className="btn btn-s" onClick={() => openModal('flight-deadline', null, flight.id)}>+ Aggiungi scadenza</button></div>{!flight.deadlines?.length && <p className="muted">Nessuna scadenza.</p>}<div className="timeline-list">{(flight.deadlines || []).map((deadline) => <div key={deadline.id} className="timeline-item"><div><strong>{deadline.title}</strong><div className="meta-line">{deadline.date}</div><div className="muted">{deadline.notes}</div></div><div className="actions-row"><a className="btn btn-s" href={googleCalendarLink({ title: `${selectedTrip.name} · ${deadline.title}`, startDate: deadline.date, endDate: deadline.date, details: deadline.notes, location: `${flight.from} → ${flight.to}` })} target="_blank" rel="noreferrer">Google Calendar</a><button className="btn btn-s btn-d" onClick={() => deleteFlightDeadline(selectedTrip.id, flight.id, deadline.id)}>Elimina</button></div></div>)}</div></div></div>)}</div></section>}

      {activeTab === 'hotels' && <section><div className="sh wrap"><div className="st">Hotel</div><button className="btn btn-p" onClick={() => openModal('hotel')}>+ Aggiungi hotel</button></div>{!selectedTrip.hotels.length && <EmptyState icon="ti ti-building-skyscraper" text="Nessun hotel — clicca Aggiungi" />}<div className="grid-cards cols-1">{selectedTrip.hotels.map((hotel) => <div key={hotel.id} className="card stack-card"><div className="card-head between wrap"><div><div className="trip-nm small">{hotel.name || 'Hotel'}</div><div className="meta-line">{hotel.address}</div></div><div className="meta-group"><span>{hotel.checkIn}</span><span>{hotel.checkOut}</span></div></div><div className="detail-grid cols-3"><div><strong>Telefono</strong><span>{hotel.phone || '—'}</span></div><div><strong>Pagato</strong><span>{hotel.paidAmount ? `€ ${hotel.paidAmount}` : '—'}</span></div><div><strong>Da pagare</strong><span>{hotel.dueAmount ? `€ ${hotel.dueAmount}` : '—'}</span></div></div><div className="detail-grid cols-3"><div><strong>Metodo</strong><span>{hotel.paymentMethod || '—'}</span></div><div><strong>Lat</strong><span>{hotel.lat || '—'}</span></div><div><strong>Lng</strong><span>{hotel.lng || '—'}</span></div></div><CardActions><button className="btn btn-s" onClick={() => openModal('hotel', hotel)}>Modifica</button>{hotel.bookingUrl && <a className="btn btn-s" href={hotel.bookingUrl} target="_blank" rel="noreferrer">Booking</a>}{hotel.alternateUrl && <a className="btn btn-s" href={hotel.alternateUrl} target="_blank" rel="noreferrer">Altro link</a>}{mapsLink(hotel.address, hotel.lat, hotel.lng) && <a className="btn btn-s" href={mapsLink(hotel.address, hotel.lat, hotel.lng)} target="_blank" rel="noreferrer">Maps</a>}{directionsLink(hotel.address, hotel.lat, hotel.lng) && <a className="btn btn-s" href={directionsLink(hotel.address, hotel.lat, hotel.lng)} target="_blank" rel="noreferrer">Indicazioni</a>}<button className="btn btn-s btn-d" onClick={() => deleteHotel(selectedTrip.id, hotel.id)}>Elimina</button></CardActions>{hotel.cancellationDate && <div className="chip-card"><span>Cancellazione prenotazione: {hotel.cancellationDate}</span><a className="btn btn-s" href={googleCalendarLink({ title: `${selectedTrip.name} · Scadenza cancellazione hotel`, startDate: hotel.cancellationDate, endDate: hotel.cancellationDate, details: `Controlla cancellazione prenotazione per ${hotel.name}`, location: hotel.address })} target="_blank" rel="noreferrer">Promemoria Calendar</a></div>}<div className="subsection"><div className="subsection-head between wrap"><h4>Scadenze hotel</h4><button className="btn btn-s" onClick={() => openModal('hotel-deadline', null, hotel.id)}>+ Aggiungi scadenza</button></div>{!hotel.deadlines?.length && <p className="muted">Nessuna scadenza hotel.</p>}<div className="timeline-list">{(hotel.deadlines || []).map((deadline) => <div key={deadline.id} className="timeline-item"><div><strong>{deadline.title}</strong><div className="meta-line">{deadline.date}</div><div className="muted">{deadline.notes}</div></div><div className="actions-row"><a className="btn btn-s" href={googleCalendarLink({ title: `${selectedTrip.name} · ${deadline.title}`, startDate: deadline.date, endDate: deadline.date, details: deadline.notes, location: hotel.address })} target="_blank" rel="noreferrer">Google Calendar</a><button className="btn btn-s btn-d" onClick={() => deleteHotelDeadline(selectedTrip.id, hotel.id, deadline.id)}>Elimina</button></div></div>)}</div></div></div>)}</div></section>}

      {activeTab === 'logistics' && <section><div className="grid-cards cols-2"><div className="card stack-card"><div className="sh wrap"><div className="st small-title">Parcheggi</div><button className="btn btn-s btn-p" onClick={() => openModal('parking')}>+ Parcheggio</button></div>{!(selectedTrip.parkingReservations || []).length && <EmptyState icon="ti ti-parking" text="Nessun parcheggio prenotato." />}<div className="timeline-list">{(selectedTrip.parkingReservations || []).map((park) => <div key={park.id} className="timeline-item"><div><strong>{park.name}</strong><div className="meta-line">{park.address}</div><div className="muted">{park.dateFrom} → {park.dateTo}</div><div className="muted">{park.cost ? `€ ${park.cost}` : ''} {park.notes}</div></div><div className="actions-row">{park.bookingUrl && <a className="btn btn-s" href={park.bookingUrl} target="_blank" rel="noreferrer">Prenotazione</a>}{directionsLink(park.address) && <a className="btn btn-s" href={directionsLink(park.address)} target="_blank" rel="noreferrer">Indicazioni</a>}<button className="btn btn-s btn-d" onClick={() => deleteParkingReservation(selectedTrip.id, park.id)}>Elimina</button></div></div>)}</div></div><div className="card stack-card"><div className="sh wrap"><div className="st small-title">Affitto auto</div><button className="btn btn-s btn-p" onClick={() => openModal('car-rental')}>+ Noleggio</button></div>{!(selectedTrip.carRentals || []).length && <EmptyState icon="ti ti-car" text="Nessun noleggio auto inserito." />}<div className="timeline-list">{(selectedTrip.carRentals || []).map((car) => <div key={car.id} className="timeline-item"><div><strong>{car.company}</strong><div className="meta-line">Ritiro: {car.pickupPlace} · {car.pickupDate}</div><div className="meta-line">Consegna: {car.dropoffPlace} · {car.dropoffDate}</div><div className="muted">Costo: {car.cost ? `€ ${car.cost}` : '—'} · Deposito: {car.deposit ? `€ ${car.deposit}` : '—'}</div><div className="muted">{car.notes}</div></div><div className="actions-row">{car.bookingUrl && <a className="btn btn-s" href={car.bookingUrl} target="_blank" rel="noreferrer">Prenotazione</a>}<button className="btn btn-s btn-d" onClick={() => deleteCarRental(selectedTrip.id, car.id)}>Elimina</button></div></div>)}</div></div></div></section>}

      {activeTab === 'diary' && <section><div className="sh wrap"><div className="st">Diario smart</div><div className="actions-row"><button className="btn btn-p" onClick={() => openModal('diary-day')}>+ Giorno</button><button className="btn" onClick={() => openModal('diary-place')}>+ Luogo smart</button><button className="btn" onClick={() => openModal('diary-media')}>+ Media link</button></div></div><div className="grid-cards cols-2"><div className="card stack-card"><div className="card-title">Giorni</div>{!selectedTrip.travelDiary?.days?.length && <EmptyState icon="ti ti-calendar-event" text="Nessun giorno inserito." />}<div className="timeline-list">{(selectedTrip.travelDiary?.days || []).map((day) => <div key={day.id} className="timeline-item"><div><strong>{day.title || 'Giorno di viaggio'}</strong><div className="meta-line">{day.date}</div><div className="muted">{day.notes}</div></div><button className="btn btn-s btn-d" onClick={() => deleteDiaryDay(selectedTrip.id, day.id)}>Elimina</button></div>)}</div></div><div className="card stack-card"><div className="card-title">Luoghi salvati</div>{!selectedTrip.travelDiary?.places?.length && <EmptyState icon="ti ti-map-pin" text="Nessun luogo salvato." />}<div className="timeline-list">{(selectedTrip.travelDiary?.places || []).map((place) => <div key={place.id} className="timeline-item"><div className="place-card-main">{place.imageUrl ? <img className="place-thumb" src={place.imageUrl} alt={place.name} /> : <div className="place-thumb placeholder"><i className="ti ti-photo" /></div>}<div><strong>{place.name}</strong><div className="meta-line">{place.type}</div><div className="muted">{place.address}</div><div className="muted">{place.notes}</div><div className="muted">Rating: {place.rating || 'n.d.'} {place.reviewNote ? `· ${place.reviewNote}` : ''}</div></div></div><div className="actions-row">{place.url && <a className="btn btn-s" href={place.url} target="_blank" rel="noreferrer">Apri</a>}{mapsLink(place.address, place.lat, place.lng) && <a className="btn btn-s" href={mapsLink(place.address, place.lat, place.lng)} target="_blank" rel="noreferrer">Maps</a>}{directionsLink(place.address, place.lat, place.lng) && <a className="btn btn-s" href={directionsLink(place.address, place.lat, place.lng)} target="_blank" rel="noreferrer">Indicazioni</a>}<button className="btn btn-s btn-d" onClick={() => deleteDiaryPlace(selectedTrip.id, place.id)}>Elimina</button></div></div>)}</div></div></div><section className="card stack-card"><div className="card-title">Guide, book, social e video</div>{!(selectedTrip.travelDiary?.mediaLinks || []).length && <EmptyState icon="ti ti-book" text="Nessun media link salvato." />}<div className="media-grid">{(selectedTrip.travelDiary?.mediaLinks || []).map((media) => <article key={media.id} className="media-card">{media.thumbnail ? <img className="media-thumb" src={media.thumbnail} alt={media.title} /> : <div className="media-thumb placeholder"><i className="ti ti-link" /></div>}<div className="media-body"><div className="pill-row"><span className="pill pill-a">{media.type}</span><span className="pill">{media.source || 'link'}</span></div><h4>{media.title}</h4><p>{media.notes}</p><div className="actions-row"><a className="btn btn-s" href={media.url} target="_blank" rel="noreferrer">Apri</a><button className="btn btn-s btn-d" onClick={() => deleteDiaryMedia(selectedTrip.id, media.id)}>Elimina</button></div></div></article>)}</div></section></section>}

      {activeTab === 'checklist' && <section><div className="sh wrap"><div className="st">Reminder cose da portare</div><div className="meta-line">Checklist iniziale già caricata, ma completamente modificabile.</div></div><div className="grid-cards cols-2">{(selectedTrip.packingChecklist || []).map((group) => <div key={group.id} className={`card stack-card checklist-group ${group.color || ''}`}><div className="subsection-head between wrap"><h4>{group.category}</h4><button className="btn btn-s" onClick={() => openModal('checklist-item', null, group.id)}>+ Voce</button></div><div className="checklist-items">{group.items.map((item) => <label key={item.id} className={`check-item ${item.done ? 'done' : ''}`}><input type="checkbox" checked={item.done} onChange={() => toggleChecklistItem(selectedTrip.id, group.id, item.id)} /><span>{item.label}</span><button type="button" className="btn-icon danger" onClick={() => removeChecklistItem(selectedTrip.id, group.id, item.id)}><i className="ti ti-x" /></button></label>)}</div></div>)}</div></section>}
    </>}

    {modal?.type === 'trip' && <Modal title={modal.item ? 'Modifica viaggio' : 'Aggiungi viaggio'} onClose={closeModal}><form onSubmit={submitTrip}><div className="fg"><label className="fl">Nome</label><input className="fi" name="name" defaultValue={modal.item?.name || ''} required /></div><div className="fg"><label className="fl">Stato</label><select className="fi" name="status" defaultValue={modal.item?.status || 'planning'}>{TRIP_STATUS_OPTIONS.map((status) => <option key={status.value} value={status.value}>{status.label}</option>)}</select></div><div className="fr responsive-2"><div className="fg"><label className="fl">Dal</label><input className="fi" type="date" name="dateFrom" defaultValue={modal.item?.dateFrom || ''} /></div><div className="fg"><label className="fl">Al</label><input className="fi" type="date" name="dateTo" defaultValue={modal.item?.dateTo || ''} /></div></div><div className="fa"><button type="button" className="btn" onClick={closeModal}>Annulla</button><button type="submit" className="btn btn-p">Salva</button></div></form></Modal>}
    {modal?.type === 'flight' && <Modal title={modal.item ? 'Modifica volo' : 'Aggiungi volo'} onClose={closeModal}><form onSubmit={submitFlight}><div className="fg"><label className="fl">Compagnia</label><select className="fi" name="company" defaultValue={modal.item?.company || 'Ryanair'}>{Object.keys(AIRLINE_DIRECTORY).map((name) => <option key={name} value={name}>{name}</option>)}</select></div><div className="fr responsive-2"><div className="fg"><label className="fl">Da</label><input className="fi" name="from" defaultValue={modal.item?.from || ''} /></div><div className="fg"><label className="fl">A</label><input className="fi" name="to" defaultValue={modal.item?.to || ''} /></div></div><div className="fr responsive-3"><div className="fg"><label className="fl">Data</label><input className="fi" type="date" name="date" defaultValue={modal.item?.date || ''} /></div><div className="fg"><label className="fl">Partenza</label><input className="fi" type="time" name="departureTime" defaultValue={modal.item?.departureTime || ''} /></div><div className="fg"><label className="fl">Arrivo</label><input className="fi" type="time" name="arrivalTime" defaultValue={modal.item?.arrivalTime || ''} /></div></div><div className="fr responsive-3"><div className="fg"><label className="fl">Numero volo</label><input className="fi" name="flightNumber" defaultValue={modal.item?.flightNumber || ''} /></div><div className="fg"><label className="fl">Booking ref</label><input className="fi" name="bookingRef" defaultValue={modal.item?.bookingRef || ''} /></div><div className="fg"><label className="fl">Costo acquisto</label><input className="fi" name="purchaseCost" defaultValue={modal.item?.purchaseCost || ''} /></div></div><div className="fa"><button type="button" className="btn" onClick={closeModal}>Annulla</button><button type="submit" className="btn btn-p">Salva</button></div></form></Modal>}
    {modal?.type === 'flight-bag' && <Modal title="Aggiungi bagaglio al volo" onClose={closeModal}><form onSubmit={submitFlightBag}><div className="fg"><label className="fl">Bagaglio</label><input className="fi" name="label" placeholder="Cabina 10kg, Stiva 20kg..." /></div><div className="fr responsive-2"><div className="fg"><label className="fl">Quantità</label><input className="fi" name="qty" defaultValue="1" /></div><div className="fg"><label className="fl">Costo acquisto</label><input className="fi" name="cost" /></div></div><div className="fa"><button type="button" className="btn" onClick={closeModal}>Annulla</button><button type="submit" className="btn btn-p">Salva</button></div></form></Modal>}
    {modal?.type === 'flight-deadline' && <Modal title="Aggiungi scadenza volo" onClose={closeModal}><form onSubmit={submitFlightDeadline}><div className="fg"><label className="fl">Titolo</label><input className="fi" name="title" /></div><div className="fg"><label className="fl">Data</label><input className="fi" type="date" name="date" /></div><div className="fg"><label className="fl">Note</label><textarea className="fi" rows="4" name="notes" /></div><div className="fa"><button type="button" className="btn" onClick={closeModal}>Annulla</button><button type="submit" className="btn btn-p">Salva</button></div></form></Modal>}
    {modal?.type === 'hotel' && <Modal title={modal.item ? 'Modifica hotel' : 'Aggiungi hotel'} onClose={closeModal}><form onSubmit={submitHotel}><div className="fg"><label className="fl">Nome</label><input className="fi" name="name" defaultValue={modal.item?.name || ''} /></div><div className="fr responsive-2"><div className="fg"><label className="fl">Telefono</label><input className="fi" name="phone" defaultValue={modal.item?.phone || ''} /></div><div className="fg"><label className="fl">Indirizzo</label><input className="fi" name="address" defaultValue={modal.item?.address || ''} /></div></div><div className="fr responsive-2"><div className="fg"><label className="fl">Latitudine</label><input className="fi" name="lat" defaultValue={modal.item?.lat || ''} /></div><div className="fg"><label className="fl">Longitudine</label><input className="fi" name="lng" defaultValue={modal.item?.lng || ''} /></div></div><div className="fr responsive-2"><div className="fg"><label className="fl">Link Booking</label><input className="fi" name="bookingUrl" defaultValue={modal.item?.bookingUrl || ''} /></div><div className="fg"><label className="fl">Altro link</label><input className="fi" name="alternateUrl" defaultValue={modal.item?.alternateUrl || ''} /></div></div><div className="fr responsive-2"><div className="fg"><label className="fl">Check-in</label><input className="fi" type="date" name="checkIn" defaultValue={modal.item?.checkIn || ''} /></div><div className="fg"><label className="fl">Check-out</label><input className="fi" type="date" name="checkOut" defaultValue={modal.item?.checkOut || ''} /></div></div><div className="fr responsive-3"><div className="fg"><label className="fl">Importo pagato</label><input className="fi" name="paidAmount" defaultValue={modal.item?.paidAmount || ''} /></div><div className="fg"><label className="fl">Da pagare</label><input className="fi" name="dueAmount" defaultValue={modal.item?.dueAmount || ''} /></div><div className="fg"><label className="fl">Pagamento</label><input className="fi" name="paymentMethod" defaultValue={modal.item?.paymentMethod || ''} placeholder="Carta, contanti, bonifico..." /></div></div><div className="fg"><label className="fl">Data cancellazione prenotazione</label><input className="fi" type="date" name="cancellationDate" defaultValue={modal.item?.cancellationDate || ''} /></div><div className="fa"><button type="button" className="btn" onClick={closeModal}>Annulla</button><button type="submit" className="btn btn-p">Salva</button></div></form></Modal>}
    {modal?.type === 'hotel-deadline' && <Modal title="Aggiungi scadenza hotel" onClose={closeModal}><form onSubmit={submitHotelDeadline}><div className="fg"><label className="fl">Titolo</label><input className="fi" name="title" /></div><div className="fg"><label className="fl">Data</label><input className="fi" type="date" name="date" /></div><div className="fg"><label className="fl">Note</label><textarea className="fi" rows="4" name="notes" /></div><div className="fa"><button type="button" className="btn" onClick={closeModal}>Annulla</button><button type="submit" className="btn btn-p">Salva</button></div></form></Modal>}
    {modal?.type === 'parking' && <Modal title="Prenotazione parcheggio" onClose={closeModal}><form onSubmit={submitParking}><div className="fg"><label className="fl">Parcheggio</label><input className="fi" name="name" /></div><div className="fg"><label className="fl">Indirizzo</label><input className="fi" name="address" /></div><div className="fr responsive-2"><div className="fg"><label className="fl">Dal</label><input className="fi" type="date" name="dateFrom" /></div><div className="fg"><label className="fl">Al</label><input className="fi" type="date" name="dateTo" /></div></div><div className="fr responsive-2"><div className="fg"><label className="fl">Costo</label><input className="fi" name="cost" /></div><div className="fg"><label className="fl">Link prenotazione</label><input className="fi" name="bookingUrl" /></div></div><div className="fg"><label className="fl">Note</label><textarea className="fi" rows="4" name="notes" /></div><div className="fa"><button type="button" className="btn" onClick={closeModal}>Annulla</button><button type="submit" className="btn btn-p">Salva</button></div></form></Modal>}
    {modal?.type === 'car-rental' && <Modal title="Affitto auto a destinazione" onClose={closeModal}><form onSubmit={submitCarRental}><div className="fg"><label className="fl">Compagnia</label><input className="fi" name="company" /></div><div className="fr responsive-2"><div className="fg"><label className="fl">Ritiro</label><input className="fi" name="pickupPlace" /></div><div className="fg"><label className="fl">Consegna</label><input className="fi" name="dropoffPlace" /></div></div><div className="fr responsive-2"><div className="fg"><label className="fl">Data ritiro</label><input className="fi" type="date" name="pickupDate" /></div><div className="fg"><label className="fl">Data consegna</label><input className="fi" type="date" name="dropoffDate" /></div></div><div className="fr responsive-3"><div className="fg"><label className="fl">Costo</label><input className="fi" name="cost" /></div><div className="fg"><label className="fl">Deposito</label><input className="fi" name="deposit" /></div><div className="fg"><label className="fl">Link prenotazione</label><input className="fi" name="bookingUrl" /></div></div><div className="fg"><label className="fl">Note utili</label><textarea className="fi" rows="4" name="notes" placeholder="patente, carta richiesta, politica carburante, seggiolino, franchigia..." /></div><div className="fa"><button type="button" className="btn" onClick={closeModal}>Annulla</button><button type="submit" className="btn btn-p">Salva</button></div></form></Modal>}
    {modal?.type === 'diary-day' && <Modal title="Aggiungi giorno al diario" onClose={closeModal}><form onSubmit={submitDiaryDay}><div className="fg"><label className="fl">Data</label><input className="fi" type="date" name="date" /></div><div className="fg"><label className="fl">Titolo</label><input className="fi" name="title" /></div><div className="fg"><label className="fl">Note giornata</label><textarea className="fi" rows="4" name="notes" /></div><div className="fa"><button type="button" className="btn" onClick={closeModal}>Annulla</button><button type="submit" className="btn btn-p">Salva</button></div></form></Modal>}
    {modal?.type === 'diary-place' && <Modal title="Aggiungi luogo smart" onClose={closeModal}><form onSubmit={submitPlace}><div className="fg"><label className="fl">Tipo luogo</label><select className="fi" name="type" defaultValue="attrazione"><option value="museo">Museo</option><option value="attrazione">Attrazione</option><option value="bar">Bar</option><option value="ristorante">Ristorante</option><option value="shop">Shopping</option><option value="altro">Altro</option></select></div><div className="fg"><label className="fl">Nome</label><input className="fi" name="name" /></div><div className="fg"><label className="fl">Indirizzo</label><input className="fi" name="address" placeholder="oppure geolocalizza e salva coordinate" /></div><div className="fr responsive-2"><div className="fg"><label className="fl">Latitudine</label><input className="fi" name="lat" /></div><div className="fg"><label className="fl">Longitudine</label><input className="fi" name="lng" /></div></div><div className="fa left"><button type="button" className="btn" onClick={fillCurrentPosition}>Usa posizione attuale</button></div><div className="fr responsive-2"><div className="fg"><label className="fl">URL Google Maps o sito</label><input className="fi" name="url" /></div><div className="fg"><label className="fl">Miniatura URL</label><input className="fi" name="imageUrl" /></div></div><div className="fr responsive-2"><div className="fg"><label className="fl">Rating</label><input className="fi" name="rating" placeholder="4.6" /></div><div className="fg"><label className="fl">Recensione / memo</label><input className="fi" name="reviewNote" placeholder="ottimo aperitivo, coda lunga..." /></div></div><div className="fg"><label className="fl">Note</label><textarea className="fi" rows="4" name="notes" /></div><div className="fa"><button type="button" className="btn" onClick={closeModal}>Annulla</button><button type="submit" className="btn btn-p">Salva</button></div></form></Modal>}
    {modal?.type === 'diary-media' && <Modal title="Aggiungi book / guide / social / video" onClose={closeModal}><form onSubmit={submitMedia}><div className="fg"><label className="fl">Tipo</label><select className="fi" name="type" defaultValue="link"><option value="book">Book / guida</option><option value="instagram">Instagram</option><option value="facebook">Facebook</option><option value="youtube">YouTube</option><option value="link">Altro link</option></select></div><div className="fg"><label className="fl">Titolo</label><input className="fi" name="title" /></div><div className="fg"><label className="fl">URL</label><input className="fi" name="url" required /></div><div className="fr responsive-2"><div className="fg"><label className="fl">Source / autore</label><input className="fi" name="source" placeholder="Lonely Planet, creator, pagina..." /></div><div className="fg"><label className="fl">Miniatura URL</label><input className="fi" name="thumbnail" placeholder="su YouTube provo a generarla dal link" /></div></div><div className="fg"><label className="fl">Note</label><textarea className="fi" rows="4" name="notes" /></div><div className="fa"><button type="button" className="btn" onClick={closeModal}>Annulla</button><button type="submit" className="btn btn-p">Salva</button></div></form></Modal>}
    {modal?.type === 'checklist-item' && <Modal title="Aggiungi voce checklist" onClose={closeModal}><form onSubmit={submitChecklistItem}><div className="fg"><label className="fl">Nuova voce</label><input className="fi" name="label" placeholder="es. farmaco bimbo, telo extra, caricatore smartwatch..." /></div><div className="fa"><button type="button" className="btn" onClick={closeModal}>Annulla</button><button type="submit" className="btn btn-p">Salva</button></div></form></Modal>}
  </div>
}
